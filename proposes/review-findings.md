# FS6 multi-agent review — consolidated findings register

Five fresh-context reviews (architect, security, performance, tester, devils-advocate), 2026-08-28, repo frozen @ `9ee38ba`. Raw reports: `~/.pi/tmp/fs6-reviews/`. Verdicts: **architect SHIP-WITH-FIXES · security SHIP-WITH-FIXES · performance SHIP · tester SHIP-WITH-FIXES · devils-advocate SHIP-WITH-FIXES**.

**Builder verification policy:** every P0/P1 claim below was independently re-verified by the builder against source/live files before entering this register (column "V" = VERIFIED by builder; "○" = agent-claimed, spot-consistent, not re-derived). Code is FROZEN — this register TRIAGES; it does not apply code fixes. Doc-lane mitigations (battery probes, runbook steps) are already applied.

## Register

> **Fix-window outcome (2026-08-28, orchestrator-approved):** R2+R7, R8 → FIXED (`02b0853`); R6 → FIXED (`0d137f9`); R4 → FIXED (`74a3100`); R3 → FIXED (`5c11f3c`). 218/218 tests + typecheck clean. R1 stays runbook-mandatory (no code). R5 → **accepted-followup, post-rollout design decision** — parity-with-today: the current zackify+bridge stack has the same class of hole (model-writable config scopes with no protection; zackify gated no reads at all), so shipping does not regress; the follow-up designs floor-protection (write-side) for the six config scopes + `.pi/agents/`. R9/R10 stay parked.

| ID | Sev | Source(s) | Finding | V | Triage |
|---|---|---|---|---|---|
| **R1** | **P0** | devils-advocate #1 | **Sandbox wrap-order kills every bash rule post-rollout.** pi dispatches `tool_call` sequentially over one shared event in packages order (runner.js `emitToolCall` — verified); pi-claude-sandbox (personal idx 20, work idx 22 — verified) mutates `input.command` to the sandbox-exec wrapper (verified at its wrap site; `enabled:true` globally — verified); `pi install` APPENDS pi-permissions after it; bash rules are anchored regexes → `Bash(git push --force *)` etc. silently stop matching. Today's bridge/zackify load BEFORE the sandbox (verified) — strict regression. Battery can't catch it (probe launches without sandbox). | V | **BLOCKER — runbook fix applied (§4.5 reorder + probe L); no code change needed. Optionally harden later: wrapper-tolerant matching (parked class).** |
| **R2** | **P0** | security #1 + tester #1 (independent) | **Safety-floor evasion under bypass startup default.** Literal-substring protected-path check misses `$HOME/.ssh/id_rsa` (env indirection); `rmRfPatterns` miss `--recursive --force` long flags; `resolveAbsoluteShellTarget` returns raw string so `//etc`, `/etc/../etc` defeat CRITICAL_DIRS; relative `rm -rf ../../../..` unresolvable. In `bypassPermissions` (D4 default) the floor is the only gate. zackify-parity gaps (verbatim port) — not a regression vs today, but sole-layer tomorrow. | V (all four variants read directly from safety.ts) | **BLOCKER-class — recommend code fix pre-push (~15 lines + adversarial test table): expand `$HOME`/`${HOME}`/`$USER` pre-check, `path.resolve()` absolute rm targets, long-flag patterns. Orchestrator decision (freeze).** |
| **R3** | **P1** | security #2 | **Children never inject grandchild mode.** Child branch (index.ts:396-424) has zero `allowAgentSpawn` calls (verified) — a child spawning a grandchild passes the model-authored `input.extensionBindings` verbatim or nothing → grandchild falls to fail-open bypass floor. Escalation: non-bypass child + `Agent` allow rule → grandchild at bypass. | V | **Fix pre/at-push (mirror the 3 child allow-exits; ~5 lines + tests).** |
| **R4** | **P1** | security #3 | **Gateway bare-name unresolved → whole-tool `mcp`** (canonicalize.ts:344 `return undefined` → family `other` — verified): deny `mcp__S` and even deny `mcp__*` both miss; lazy servers pre-first-connect are the reachable state; "Always" persists a blanket `mcp` allow. | V | **Fix pre/at-push (server-known → fall back `mcp__S`; refuse persisting whole-tool `mcp`).** |
| **R5** | **P1** | security #4 | **Config self-modification under acceptEdits.** Model-writable `.pi/permissions.local.json` (auto-allowed in acceptEdits) hot-reloads to strip deny rules / replace protectedPaths; writable `.pi/agents/*.md` frontmatter `permissionMode: bypassPermissions` beats inheritance at next spawn. Prompt injection → acceptEdits-bypass in one turn. | ○ (mechanisms all read-verified; end-to-end not exercised) | **Design-sensitive — orchestrator decision: floor-protect the 6 config scopes + `.pi/agents/` (write-side) vs exclude from acceptEdits auto-allow. Note: persist.ts writes bypass the tool pipeline, so protecting the persist target does NOT break "Always".** |
| **R6** | **P1** | architect #1 + security #7 (dup) | **Unparsable scope JSON = silent rule loss.** `readScope` catch → `null` (absent) — deny rules vanish with no ⚠/issue; hot-reload can make it sticky (clearDirty before awaited reload). Contradicts "never silently skipped" at file granularity. | V | **Fix pre/at-push (~10 lines: ENOENT vs parse-failure split → RuleIssue). Existing test pin ("deny gone, no crash") unaffected per architect.** |
| **R7** | **P1** | tester #1/#2/#3 | **Test gaps on the security boundary:** no adversarial floor table (R2 sailed through), `..`-traversal canonicalization untested, real-`fs.watch` path only manually covered (rollout gate's weakest oracle). | V (gaps confirmed by absence) | **Land WITH R2 fix (adversarial table pins every variant's verdict) + R6; env-gated `PI_PERM_REAL_WATCH` integration test for the harness tab.** |
| **R8** | **P1** | devils-advocate #2 + security residual | **Profile-auth files unprotected** (`~/.pi/personal/auth.json`, `~/.pi/work/auth.json` — read free in every mode; work file holds bedrock creds). Already decided Option A / parking-lot (orchestrator 2026-08-28); both reviews independently escalate. 2-line default addition is the cheapest close. | V (files verified live earlier) | **Re-openable: orchestrator may fold the 2-line `DEFAULT_PROTECTED_PATHS` addition into the R2 fix window (one commit, one decision).** |
| **R9** | P2 | tester #4-#9, perf #1-#5, architect #2-#5, security #5/#6/#8/#9, devils-advocate #3-#9 | Assorted: in-flight ask dedupe; destructive persist overwrite (.bak); watcher startup-throw never retried (add dirty in sync catch); alias-resolution gap (`Agent(worker)` bypassed by alias spawn); action-bearing subagent calls; case-sensitive paths on case-insensitive FS; persist tmp collision; loader O(n²) cold dedupe; uncached regex compiles (14µs worst — measured fine); sync frontmatter scan (2-5ms/spawn); `getPiAgentDir` home; harness type-fidelity; `mcp.json` project-scope untested at factory level; default-mode prompt surface wider than zackify (awareness); two-session cache wipe; Claude-global hot-reload coupling; monorepo swap verification; interim-window rollback. | ○ | **Park to post-rollout backlog. Doc-lane items already applied: battery floor probe (was DA #3), §4.5 reorder, rollback line, §6 verification, README coupling sentence.** |
| R10 | P3 | architect #6/#7 | relative protectedPaths resolve vs process.cwd; `reference/` retention window | ○ | Park; drop `reference/` after bridge retirement per its own README. |

## Cleared by review (no action)

- D4 bypass startup default = exact parity with today's effective posture (zackify DEFAULT_MODE + bridge mode-independent deny/ask) — devils-advocate cleared with evidence.
- H3 protectedPaths no-op verdict re-verified correct by two lanes.
- Status-slot conflicts (only retiring extensions touch `permissions`), `before_agent_start` coexistence with pi-tool-prompt, plan-mode drop — all cleared with evidence.
- Evaluator order/short-circuits, parent-side binding forgery protection, path `..` normalization, ask/persist loop, watch coalescing — security-verified clean.
- Performance: SHIP, zero measurable findings (worst hot-path 14µs/call; reload ~1ms; startup ~1ms) — no-debounce hot-reload design validated by measurement.

## Recommended gate for push (orchestrator/user decision)

1. **R1** — no code; runbook §4.5 (reorder) + probe L applied. **Must not be skipped at install.**
2. **R2 + R7 + R8(one-commit option)** — one code window before push (~25 lines + tests): floor hardening ($HOME/long-flags/resolve), adversarial table, optional 2-line profile-auth defaults.
3. **R3, R4, R6** — small code fixes, same window or immediately post-push pre-install.
4. **R5** — needs a design decision (protect-config-files mechanics); can follow post-rollout if R2 lands (it narrows the window but doesn't close R5).
5. Everything else parks.

If the orchestrator prefers zero code churn pre-push: R2/R3/R4/R6 are all zackify-parity-or-better (no regression vs today's stack) EXCEPT they become the sole layer — the decision is risk-appetite, not correctness. R1 remains mandatory regardless (it IS a regression).
