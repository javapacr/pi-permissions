{
  "name": "pi-permissions",
  "version": "0.1.0",
  "description": "Claude-Code-parity permission engine for pi: rule grammar, 4 modes, dual-source config, child enforcement. FS0 seed fork of @zackify/pi-claude-permissions v1.0.6 with the pi-claude-permissions-bridge engine grafted as reference.",
  "keywords": [
    "pi-package",
    "pi",
    "permissions"
  ],
  "license": "MIT",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/javapacr/pi-permissions.git"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  }
}
