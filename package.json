{
  "name": "pi-permissions",
  "version": "0.1.0",
  "description": "Claude-Code-parity permission engine for pi: rule grammar (bash/path/webfetch globs, MCP dual-surface canonicalization, Agent gating), 4 modes, dual-source Claude+pi config with hot-reload, single-ask UX, read-gated safety floor, pi-subagents child enforcement.",
  "keywords": [
    "pi-package",
    "pi",
    "permissions",
    "claude-code"
  ],
  "license": "MIT",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/javapacr/pi-permissions.git"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  }
}
